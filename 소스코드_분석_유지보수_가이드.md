# MyBudgetBook 소스코드 분석 및 유지보수 가이드

> 작성일: 2026-05-19  
> 목적: 현재 구현된 가계부 웹앱의 소스 구조, 기능 흐름, 확장 포인트, 검증 방법을 빠르게 파악하기 위한 유지보수 문서

## 1. 프로젝트 개요

MyBudgetBook은 반응형 웹 기반 가계부입니다. 모바일 브라우저/PWA 사용을 우선 고려하며, 백엔드는 Spring Boot REST API, 프론트엔드는 React + Vite + Tailwind CSS로 구성되어 있습니다.

### 기술 스택

| 영역 | 기술 |
|---|---|
| Backend | Java 21, Spring Boot 3.5.0, Spring Web, Spring Data JPA, Validation, Actuator |
| DB | H2 file DB (`backend/data/mybudget_codex.mv.db`), MySQL 드라이버 포함 |
| Auth | PBKDF2 비밀번호 해시, HMAC-SHA256 서명 토큰, MVC Interceptor |
| API 문서 | springdoc-openapi `2.8.0`, Swagger UI |
| Frontend | React 19, TypeScript 6, Vite 8, React Router 7 |
| 상태/서버 데이터 | React Query, Zustand |
| UI | Tailwind CSS v4, lucide-react, Recharts |
| PWA | vite-plugin-pwa, Workbox. 정적 앱 셸만 캐시하고 API 요청은 캐시 대상에서 제외 |
| 개발 환경 | 프로젝트 로컬 `tools/` 기반 Java/Node 격리 환경 |

## 2. 루트 폴더 구조

```text
budget_book_codex/
├── backend/                         # Spring Boot 백엔드
├── frontend/                        # React/Vite 프론트엔드
├── tools/                           # 프로젝트 로컬 개발 도구(Java, Node)
├── activate-env.sh                  # tools/ 환경 활성화
├── check-dev-env.sh                 # 개발 환경 점검
├── install-dev-env.sh               # 개발 도구 설치 스크립트
├── setup-project.sh                 # Phase 2 보일러플레이트 생성
├── setup-phase4-domain.sh           # 도메인/Repository/seed 생성
├── setup-phase5-api.sh              # REST API 생성
├── setup-phase6-frontend.sh         # 프론트 화면 생성
├── setup-phase7-sms.sh              # SMS 파서 생성
├── setup-phase8-pwa.sh              # PWA 설정 생성
├── Phase*-사용가이드.md             # 단계별 사용 가이드
├── README.md                        # 프로젝트 실행/개요 문서
└── 소스코드_분석_유지보수_가이드.md # 현재 문서
```

### 보존해야 하는 폴더

| 경로 | 이유 |
|---|---|
| `backend/data/` | H2 파일 DB. 현재 거래/계좌/카테고리 데이터가 들어 있음. 이 복사본은 `mybudget_codex` DB를 사용 |
| `tools/` | 프로젝트 로컬 Java/Node. 시스템 환경과 분리하기 위해 필요 |
| `frontend/node_modules/` | 프론트 의존성. 삭제 가능하지만 재설치 필요 |
| `.gradle-cache/` | 프로젝트 내부 Gradle 캐시. 삭제 가능하지만 재다운로드 필요 |

### 정리해도 되는 파일/폴더

아래 항목은 빌드 산출물 또는 백업 파일입니다. 삭제해도 소스 기능에는 영향이 없습니다.

```bash
rm -rf frontend/dist frontend/dev-dist backend/build
rm -f backend/build.gradle.bak
rm -f setup-phase5-api.sh.bak
rm -f setup-phase6-frontend.sh.bak setup-phase6-frontend.sh.bak2 setup-phase6-frontend.sh.bak3
rm -f setup-phase8-pwa.sh.bak4 setup-phase8-pwa.sh.bak5 setup-phase8-pwa.sh.bak6
```

아래 프론트 템플릿 잔재도 현재 코드에서 참조되지 않습니다.

```text
frontend/src/App.css
frontend/src/styles/index.css
frontend/src/assets/react.svg
frontend/src/assets/vite.svg
frontend/src/assets/hero.png
```

삭제 전에는 `rg "파일명" frontend/src`로 참조 여부를 한 번 더 확인하는 편이 안전합니다.

## 3. 실행 방법

### 환경 활성화

```bash
cd ~/claude/budget_book_codex
source ./activate-env.sh
```

중요: `activate-env.sh`는 프로젝트 루트에서 실행해야 `tools/jdk-21`, `tools/node-24`를 정상 감지합니다.

### 백엔드 실행

```bash
cd ~/claude/budget_book_codex
source ./activate-env.sh
cd backend
./gradlew bootRun
```

접속:

| URL | 용도 |
|---|---|
| `http://localhost:18080/api/hello` | 백엔드 헬스 확인 |
| `http://localhost:18080/swagger-ui.html` | Swagger API 문서 |
| `http://localhost:18080/h2-console` | H2 콘솔 |

H2 콘솔 설정:

```text
JDBC URL: jdbc:h2:file:./data/mybudget_codex
User: sa
Password: 비움
```

### 프론트엔드 실행

```bash
cd ~/claude/budget_book_codex
source ./activate-env.sh
cd frontend
npm run dev
```

접속:

```text
http://localhost:15173
```

`15173` 프론트 서버는 `/api/*` 요청을 `http://localhost:18080`으로 프록시합니다. `npm run dev`, `npm run preview` 모두 같은 포트를 사용합니다.

### PWA 빌드 확인

```bash
cd ~/claude/budget_book_codex
source ./activate-env.sh
cd frontend
npm run build
npm run preview
```

접속:

```text
http://localhost:15173
```

일상 개발과 PWA 설치 확인 모두 `15173`에서 진행합니다. 개발 서버에서도 Service Worker가 등록되지만, `/api/**`는 캐시/런타임 라우팅에서 제외합니다.

## 4. 백엔드 구조 분석

### 패키지 구조

```text
backend/src/main/java/com/mybudget/backend/
├── BackendApplication.java
├── config/          # CORS, Swagger/OpenAPI 설정
├── controller/      # REST API 엔드포인트
├── domain/          # JPA Entity + enum
├── dto/             # 요청/응답 DTO, Java record 중심
├── exception/       # 전역 예외 처리
├── repository/      # Spring Data JPA Repository
├── service/         # 비즈니스 로직, 트랜잭션 처리
└── util/            # 현재는 .gitkeep만 존재
```

### 설정 파일

| 파일 | 역할 |
|---|---|
| `backend/build.gradle` | Spring Boot, JPA, H2, MySQL, Lombok, springdoc 의존성 |
| `backend/src/main/resources/application.yml` | H2 file DB, JPA update, H2 console, Actuator 설정 |
| `backend/src/test/resources/application.yml` | 테스트 전용 H2 in-memory DB. 파일 DB 잠금과 분리 |
| `backend/src/main/resources/data.sql` | 초기 계좌/카테고리/가맹점/SMS 정규식 seed 데이터 |
| `backend/src/main/java/.../config/WebConfig.java` | CORS 허용 origin (`15173`) + 인증 Interceptor 등록 |
| `backend/src/main/java/.../config/AuthInterceptor.java` | `/api/**` 인증 토큰 검증 |
| `backend/src/main/java/.../config/AuthContext.java` | 요청 처리 중 현재 사용자 ThreadLocal 보관 |
| `backend/src/main/java/.../config/OpenApiConfig.java` | Swagger UI 문서 메타데이터 |
| `backend/src/main/java/.../service/UserDataInitializer.java` | 사용자별 기본 계좌/카테고리/SMS 가맹점 규칙 생성 및 기존 단일 사용자 데이터 귀속 |

### 도메인 엔티티

| Entity | Table | 책임 |
|---|---|---|
| `User` | `users` | 사용자 계정. 이메일, PBKDF2 해시, 표시 이름 |
| `Account` | `accounts` | 현금/예금/카드 등 자산 계좌, 잔액 보유. 체크카드는 `linked_deposit_account_id`로 출금 예금을 연결. `user_id`로 사용자 분리 |
| `Category` | `categories` | 수입/지출 카테고리, parent 자기참조 지원. `user_id`로 사용자 분리 |
| `Transaction` | `transactions` | 수입/지출 거래 핵심 테이블. `balance_account_id`로 실제 잔액 반영 계좌를 별도 보관. `user_id`로 사용자 분리 |
| `Tag` | `tags` | 거래 태그. `user_id + name` 기준 |
| `TransactionAttachment` | `transaction_attachments` | 영수증/첨부 파일 메타데이터 |
| `TransactionSplit` | `transaction_splits` | 한 거래를 여러 카테고리로 분할하기 위한 구조 |
| `Transfer` | `transfers` | 예금 계좌 간 이체. 출금 계좌와 입금 계좌를 분리해 저장. `user_id`로 사용자 분리 |
| `Budget` | `budgets` | 연/월/카테고리별 예산. `user_id + year + month + category` 기준 |
| `RecurringRule` | `recurring_rules` | 고정 지출/반복 거래 규칙. `user_id`로 사용자 분리 |
| `SavingsGoal` | `savings_goals` | 목표 저축. `user_id`로 사용자 분리 |
| `FavoriteTransaction` | `favorite_transactions` | 빠른 입력용 즐겨찾기 거래. `user_id`로 사용자 분리 |
| `MerchantRule` | `merchant_rules` | 가맹점명 패턴 → 카테고리 추천. 사용자별 카테고리를 참조하므로 `user_id`로 분리 |
| `SmsParserRule` | `sms_parser_rules` | 카드사 SMS 정규식. 카드사 문자 포맷은 공통 규칙으로 유지 |

### Enum

| Enum | 값/의미 |
|---|---|
| `AccountType` | 현금/예금/카드 등 계좌 유형 |
| `CategoryKind` | `INCOME`, `EXPENSE` |
| `TransactionKind` | `INCOME`, `EXPENSE`, `TRANSFER` |
| `TransactionSource` | 수동 입력, SMS 등 거래 입력 출처 |

## 5. 백엔드 API 구조

### Controller 목록

| Controller | Base URL | 주요 기능 |
|---|---|---|
| `AuthController` | `/api/auth` | 회원가입, 로그인, 현재 사용자 조회, 로그아웃 |
| `HelloController` | `/api` | `/hello` 연결 확인 |
| `AccountController` | `/api/accounts` | 계좌 CRUD |
| `CategoryController` | `/api/categories` | 카테고리 CRUD, kind 필터 |
| `TagController` | `/api/tags` | 태그 조회/생성/삭제 |
| `TransactionController` | `/api/transactions` | 거래 조회/검색/생성/수정/삭제 |
| `TransferController` | `/api/transfers` | 계좌 간 이체 생성/조회/삭제 |
| `BudgetController` | `/api/budgets` | 월 예산 설정/조회/진행률 |
| `StatsController` | `/api/stats` | 월 요약, 카테고리별 통계, 달력 통계 |
| `RecurringRuleController` | `/api/recurring-rules` | 반복 거래 규칙 |
| `SavingsGoalController` | `/api/savings-goals` | 목표 저축 |
| `FavoriteTransactionController` | `/api/favorites` | 즐겨찾기 거래 |
| `SmsController` | `/api/sms` | SMS 파싱 |

### 주요 엔드포인트

| Method | URL | 설명 |
|---|---|---|
| `POST` | `/api/auth/register` | 회원가입 및 토큰 발급 |
| `POST` | `/api/auth/login` | 로그인 및 토큰 발급 |
| `GET` | `/api/auth/me` | 현재 로그인 사용자 조회 |
| `POST` | `/api/auth/logout` | 로그아웃 |
| `GET` | `/api/accounts` | 활성 계좌 목록 |
| `POST` | `/api/accounts` | 계좌 생성 |
| `PUT` | `/api/accounts/{id}` | 계좌 수정 |
| `DELETE` | `/api/accounts/{id}` | 계좌 삭제 |
| `GET` | `/api/accounts/{id}/activities?from=...&to=...` | 자산별 거래/이체 내역 조회 |
| `GET` | `/api/categories?kind=EXPENSE` | 카테고리 목록 |
| `POST` | `/api/categories` | 카테고리 생성 |
| `PUT` | `/api/categories/{id}` | 카테고리 수정 |
| `DELETE` | `/api/categories/{id}` | 카테고리 삭제 |
| `GET` | `/api/transactions?from=...&to=...` | 기간별 거래 조회 |
| `GET` | `/api/transactions/search?q=...` | 메모 키워드 검색 |
| `GET` | `/api/transactions/{id}` | 거래 상세 |
| `POST` | `/api/transactions` | 수입/지출 거래 생성, 계좌 잔액 자동 반영. `TRANSFER`는 `/api/transfers` 사용 |
| `PUT` | `/api/transactions/{id}` | 거래 수정, 기존 잔액 되돌린 뒤 재반영 |
| `DELETE` | `/api/transactions/{id}` | 거래 삭제, 잔액 되돌림 |
| `GET` | `/api/transfers?from=...&to=...` | 기간별 이체 조회 |
| `POST` | `/api/transfers` | 예금 간 이체 생성, 출금/입금 잔액 자동 반영 |
| `DELETE` | `/api/transfers/{id}` | 이체 삭제, 양쪽 잔액 되돌림 |
| `GET` | `/api/budgets?year=2026&month=5` | 월 예산 조회 |
| `POST` | `/api/budgets` | 예산 upsert |
| `GET` | `/api/budgets/progress?year=2026&month=5` | 예산 진행률 |
| `GET` | `/api/stats/monthly?year=2026&month=5` | 월 요약 |
| `GET` | `/api/stats/by-category?from=...&to=...&kind=EXPENSE` | 카테고리별 통계 |
| `GET` | `/api/stats/calendar?year=2026&month=5` | 달력용 일자별 통계 |
| `POST` | `/api/sms/parse` | SMS 텍스트 파싱 |

### 핵심 서비스 로직

| Service | 핵심 책임 |
|---|---|
| `AuthService` | 회원가입/로그인, 사용자 응답 DTO 생성 |
| `PasswordService` | PBKDF2 비밀번호 해시 생성/검증 |
| `TokenService` | HMAC-SHA256 토큰 생성/검증, 만료 확인 |
| `UserDataInitializer` | 신규 사용자 기본 데이터 생성, 기존 `user_id IS NULL` 데이터 최초 귀속 |
| `TransactionService` | 수입/지출 거래 CRUD, 태그 생성/연결, 실제 잔액 반영 계좌 계산. 체크카드 지출은 연결 예금에서 출금하고, 신용카드는 잔액을 직접 변경하지 않음 |
| `TransferService` | 예금 계좌 간 이체 생성/삭제 시 출금/입금 계좌 잔액 조정 |
| `BudgetService` | 예산 upsert, 월별 예산 진행률 계산 |
| `StatsService` | 월 요약, 카테고리별 집계, 달력 일별 집계 |
| `SmsParserService` | 카드사 정규식 매칭, 금액/일시/가맹점 추출, 가맹점 규칙 기반 카테고리 추천 |
| `AccountService` | 계좌 CRUD, 체크카드 연결 예금 검증, 자산별 거래/이체 내역 조합 조회 |
| `CategoryService` | 카테고리 CRUD, 수입/지출 kind 필터 |

## 6. 프론트엔드 구조 분석

### 폴더 구조

```text
frontend/src/
├── App.tsx                 # Router + QueryClient + PWA InstallBanner
├── main.tsx                # React entry
├── index.css               # Tailwind v4, Pretendard, 전역 스타일
├── api/                    # 백엔드 API 호출 함수
├── components/             # 공통 UI 컴포넌트
├── constants/              # UI 라벨/색상 상수
├── hooks/                  # PWA 설치 훅
├── routes/                 # 페이지 컴포넌트
├── stores/                 # Zustand store
└── utils/                  # 날짜/금액 포맷 유틸
```

### 라우트

| Path | Component | 기능 |
|---|---|---|
| `/login` | `LoginPage` | 로그인/회원가입. 공개 라우트 |
| `/` | `HomePage` | 이달 요약, 자산, 예산 진행률, 최근 거래 |
| `/transactions` | `TransactionsPage` | 거래 내역, 년월 조회, 검색, 일자별 그룹 |
| `/transactions/new` | `AddTransactionPage` | 수입/지출 거래 추가, 예금 간 이체 입력 |
| `/transactions/:id` | `TransactionDetailPage` | 거래 상세/수정/삭제 |
| `/calendar` | `CalendarPage` | 월 달력, 일자별 수입/지출 |
| `/stats` | `StatsPage` | 기간별 통계, 카테고리 파이 차트 |
| `/budgets` | `BudgetsPage` | 월 예산 조회/설정/진행률 |
| `/accounts` | `AccountsPage` | 계좌 조회/생성/수정/삭제 |
| `/accounts/:id` | `AccountDetailPage` | 자산별 수입/지출/이체 내역, 월 조회 |
| `/categories` | `CategoriesPage` | 카테고리 조회/생성/수정/삭제 |
| `/settings` | `SettingsPage` | 로그인 사용자, 로그아웃, 다크모드, Swagger/H2 링크 |
| `/sms` | `SmsPage` | SMS 붙여넣기 파싱/일괄 저장 |

`/login`을 제외한 앱 화면은 `ProtectedRoute`로 보호됩니다. 토큰이 없으면 현재 위치를 `state.from`에 담아 `/login`으로 이동합니다.

### API 모듈

| 파일 | 담당 API |
|---|---|
| `api/client.ts` | Axios 인스턴스, `/api` baseURL, Authorization 헤더, 401 처리 |
| `api/auth.ts` | 로그인/회원가입/현재 사용자/로그아웃 |
| `api/accounts.ts` | 계좌 CRUD, 자산별 내역 조회 |
| `api/categories.ts` | 카테고리 CRUD |
| `api/transactions.ts` | 거래 CRUD, 검색 |
| `api/transfers.ts` | 이체 API |
| `api/budgets.ts` | 예산 조회/upsert/진행률 |
| `api/stats.ts` | 월 요약/카테고리 통계/달력 통계 |
| `api/tags.ts` | 태그 API |
| `api/sms.ts` | SMS 파싱 API |
| `api/types.ts` | 공통 TypeScript 타입 |

### 공통 컴포넌트

| Component | 역할 |
|---|---|
| `AppLayout` | 모바일 하단 탭, PC 사이드바 레이아웃 |
| `PageHeader` | 페이지 제목/뒤로가기/우측 액션 |
| `MonthPicker` | 년월 선택 공통 UI |
| `AmountInput` | 금액 입력, 천 단위 포맷, 원 단위 표시 |
| `CategoryGrid` | 카테고리 선택 그리드 |
| `TxCard` | 거래 카드. 상세 이동 시 이전 목록 필터 state 보존 |
| `EmptyState` | 데이터 없음 UI |
| `Skeleton` | 로딩 플레이스홀더 |
| `InstallBanner` | PWA 설치 유도, 오프라인 준비 완료, 새 버전 적용 배너 |

### 상태 관리

| 상태 | 위치 | 설명 |
|---|---|---|
| 서버 데이터 | React Query | `useQuery`, `useMutation`, `invalidateQueries` 중심 |
| 인증 | `stores/authStore.ts` | 토큰/사용자 정보를 Zustand persist로 localStorage 저장 |
| 다크모드 | `stores/uiStore.ts` | Zustand persist로 localStorage 저장 |
| 목록 조회 조건 | URL query | 거래내역은 `year`, `month`, `q`, 자산별 내역은 `year`, `month`를 URL에 보존 |
| 상세 복귀 위치 | React Router state | `TxCard`에서 `state.from` 전달 |

## 7. 주요 기능 흐름

### 인증 흐름

```text
LoginPage
  → login() 또는 register()
  → POST /api/auth/login 또는 /api/auth/register
  → AuthService
      1. 이메일 정규화
      2. PBKDF2 비밀번호 검증 또는 해시 저장
      3. TokenService로 HMAC 토큰 발급
  → authStore에 token/user 저장
  → ProtectedRoute가 원래 요청 화면으로 복귀
  → apiClient가 모든 API 요청에 Authorization 헤더 자동 추가
```

미인증 요청은 `AuthInterceptor`에서 401로 차단됩니다. 프론트 `apiClient`는 401 수신 시 저장된 인증 정보를 지우고 `/login`으로 이동합니다.

### 사용자별 데이터 분리 흐름

```text
AuthInterceptor
  → 토큰 검증
  → UserRepository.findById()
  → UserDataInitializer.ensureUserData(user)
      1. 기존 단일 사용자 데이터(user_id null)가 있고 현재 사용자가 빈 계정이면 해당 사용자에게 귀속
      2. 사용자 계좌가 없으면 기본 계좌 2개 생성
      3. 사용자 카테고리가 없으면 지출/수입 기본 카테고리 생성
      4. 사용자 가맹점 규칙이 없으면 사용자 카테고리에 맞춰 SMS 자동분류 규칙 생성
  → AuthContext.setUser(user)
  → 각 Service에서 userId 조건으로 Repository 조회
```

서비스 계층에서는 외래키 ID를 그대로 신뢰하지 않습니다. 예를 들어 거래 생성 시 `accountId`, `categoryId`는 각각 `findByIdAndUserId(...)`로 검증한 뒤 사용합니다. 다른 사용자의 ID를 넘기면 존재하지 않는 리소스처럼 404가 반환됩니다.

### 거래 생성 흐름

```text
AddTransactionPage
  → createTransaction()
  → POST /api/transactions
  → TransactionController.create()
  → TransactionService.create()
      1. TRANSFER 요청은 차단하고 /api/transfers 사용 유도
      2. 체크카드 지출이면 Account.linkedDepositAccount를 balanceAccount로 사용
      3. 일반 수입/지출은 선택 계좌를 balanceAccount로 사용
  → Account.balance 자동 반영
  → React Query invalidate: transactions, stats, accounts
```

### 예금 간 이체 흐름

```text
AddTransactionPage(kind=TRANSFER)
  → 사용자는 보내는 예금/fromAccount, 받는 예금/toAccount를 각각 선택
  → createTransfer()
  → POST /api/transfers
  → TransferService.create()
      1. fromAccount와 toAccount가 같은지 검증
      2. 두 계좌가 모두 DEPOSIT인지 검증
      3. 출금 예금 잔액 차감, 입금 예금 잔액 증가
  → 저장 후 /accounts/{fromAccountId}로 이동해 자산별 내역 확인
```

### 체크카드 연결 예금 흐름

```text
AccountsPage
  → CHECK_CARD 계좌 생성/수정 시 연결 예금 선택
  → Account.linkedDepositAccount 저장

AddTransactionPage(kind=EXPENSE)
  → CHECK_CARD 계좌 선택
  → TransactionService.resolveBalanceAccount()
  → 거래 계좌(account)는 체크카드로 기록
  → 실제 잔액 반영 계좌(balanceAccount)는 연결 예금으로 기록
  → 연결 예금 잔액만 차감되고 체크카드 자체 잔액은 유지

CREDIT_CARD 계좌 선택
  → 거래 계좌(account)는 신용카드로 기록
  → balanceAccount는 null
  → 카드 사용 내역만 남기고 어떤 계좌 잔액도 직접 변경하지 않음
```

### 자산별 내역 조회 흐름

```text
AccountsPage
  → 각 자산의 내역 버튼
  → /accounts/{id}?year=2026&month=5
  → AccountDetailPage
  → GET /api/accounts/{id}/activities?from=...&to=...
  → AccountService.activities()
      1. transactions에서 account_id 또는 balance_account_id가 해당 계좌인 거래 조회
      2. transfers에서 from_account_id 또는 to_account_id가 해당 계좌인 이체 조회
      3. 시간 역순으로 합쳐 반환
```

### 거래 수정/삭제 흐름

```text
TransactionsPage(year/month/q)
  → TxCard 클릭, state.from에 현재 URL 저장
  → TransactionDetailPage
  → updateTransaction() 또는 deleteTransaction()
  → TransactionService에서 기존 잔액 되돌림/새 잔액 반영
  → state.from으로 복귀
```

주의: 거래 목록 필터는 URL에 저장되므로 `/transactions?year=2026&month=4` 같은 주소를 새로고침해도 같은 조회 조건이 유지됩니다.

### 예산 조회/설정 흐름

```text
BudgetsPage
  → MonthPicker로 year/month 선택
  → fetchBudgetProgress(year, month)
  → 예산 설정 모달
  → upsertBudget()
  → /api/budgets POST
  → progress query invalidate
```

### 통계 조회 흐름

```text
StatsPage
  → 시작 년월/종료 년월 선택
  → fetchByCategory(from, to, kind)
  → Recharts PieChart 표시
```

### SMS 파싱 흐름

```text
SmsPage
  → parseSms(text)
  → POST /api/sms/parse
  → SmsParserService
      1. sms_parser_rules 정규식 우선순위 순회
      2. amount/date/time/store/installment 추출
      3. store에서 누적/잔여 등 꼬리 제거
      4. merchant_rules로 카테고리 추천
  → 프론트에서 계좌 선택 후 거래 일괄 저장
```

## 8. 데이터 초기화와 seed

`backend/src/main/resources/data.sql`은 앱 시작 시 실행됩니다.

현재 seed 데이터:

| 종류 | 내용 |
|---|---|
| 계좌 | 현금 지갑, 주거래 은행 |
| 카테고리 | 지출 15개, 수입 6개 |
| 가맹점 규칙 | 스타벅스, 배달의민족, 쿠팡, 티머니 등 33개 이상 |
| SMS 정규식 | KB국민, 신한, 삼성, 현대, 롯데, 우리, 하나, BC |

주의:

```yaml
spring:
  sql:
    init:
      mode: always
```

`data.sql`이 매번 실행되므로 seed SQL은 `WHERE NOT EXISTS` 패턴을 유지해야 합니다. 그렇지 않으면 앱 재시작 시 중복 데이터나 unique 오류가 날 수 있습니다.

## 9. 추가 개발 가이드

### 백엔드에 새 기능 추가 순서

예: `PaymentSchedule` 기능 추가

1. `domain/PaymentSchedule.java` Entity 작성
2. `repository/PaymentScheduleRepository.java` 작성
3. `dto/PaymentScheduleDto.java` 작성
4. `service/PaymentScheduleService.java` 작성
5. `controller/PaymentScheduleController.java` 작성
6. Swagger UI에서 API 확인
7. 프론트 `api/paymentSchedules.ts` 작성
8. `routes/PaymentSchedulesPage.tsx` 작성
9. `App.tsx`, `AppLayout.tsx`에 라우트/메뉴 추가

### 프론트에 새 페이지 추가 순서

1. `frontend/src/api/*.ts`에 API 함수 추가
2. `frontend/src/routes/NewPage.tsx` 작성
3. `frontend/src/App.tsx`에 `<Route path="..." />` 추가
4. 필요 시 `frontend/src/components/AppLayout.tsx` 메뉴 추가
5. `npm run build`로 타입/빌드 확인

### 공통 UI 추가 기준

두 페이지 이상에서 반복되면 `components/`로 분리합니다. 현재 분리할 만한 후보:

| 후보 | 이유 |
|---|---|
| `Modal` | 계좌/카테고리/예산 모달이 각 페이지에 중복될 가능성 |
| `ConfirmButton` | 삭제 confirm 패턴 중복 |
| `FormField` | label + input + validation 메시지 패턴 |
| `KindToggle` | 수입/지출 토글 반복 |

## 10. 유지보수 주의사항

### H2 파일 DB 잠금

운영/개발 실행은 H2 파일 DB를 사용합니다. 실행 중인 앱과 H2 콘솔이 같은 파일 DB를 동시에 잡으면 파일 잠금이 날 수 있습니다.

실패 메시지 예:

```text
Database may be already in use:
backend/data/mybudget_codex.mv.db
The file is locked
```

개발 실행 중 파일 DB 잠금이 보이면:

1. 실행 중인 `./gradlew bootRun`을 먼저 `Ctrl+C`로 종료
2. H2 콘솔 브라우저 탭을 닫거나 연결 해제
3. 다시 실행

```bash
cd ~/claude/budget_book_codex
source ./activate-env.sh
cd backend
./gradlew bootRun
```

테스트는 `backend/src/test/resources/application.yml`에서 `jdbc:h2:mem:budget_book_codex_test`를 사용하도록 분리되어 있으므로 파일 DB 잠금에 영향받지 않습니다.

### PWA/Service Worker 캐시

PWA는 `15173` 포트에 통합되어 `npm run dev`, `npm run preview` 모두에서 동작합니다. 개발 모드에서는 예전 설정에서 남을 수 있는 `api-cache`만 정리합니다.

현재 Workbox 정책:

| 대상 | 정책 |
|---|---|
| 정적 앱 셸 | precache |
| CDN 폰트/정적 리소스 | CacheFirst |
| `/api/**` | 캐시/런타임 라우팅 제외 |
| `/h2-console/**` | navigate fallback 제외 |

API는 인증/쓰기 요청이 섞이므로 Service Worker가 가로채지 않습니다. 오프라인에서는 앱 화면 자체는 열릴 수 있지만, 최신 거래 조회/저장은 백엔드 연결이 필요합니다.

PWA 적용 후 프론트 변경사항이 즉시 반영되지 않으면 앱 안의 "새 버전이 준비됐습니다" 배너에서 `지금 적용`을 누릅니다. 그래도 남아 있으면 브라우저 캐시/Service Worker를 직접 정리합니다.

대응:

1. DevTools → Application → Service Workers → Unregister
2. DevTools → Application → Storage → Clear site data
3. `Cmd+Shift+R` 강력 새로고침

개발 중에는 `npm run dev`의 `15173` 포트를 사용하세요.

### Vite preview와 API

`npm run preview`는 배포 빌드 확인용이며, `npm run dev`와 같은 `15173` 포트를 사용합니다. 둘 중 하나만 실행할 수 있습니다.

권장:

| 상황 | 명령 |
|---|---|
| 기능 개발 | `npm run dev` |
| PWA 설치/캐시 확인 | `npm run dev` 또는 `npm run build && npm run preview` |

### SMS 정규식 확장

SMS 파싱 실패가 나오면 우선 H2 콘솔에서 `sms_parser_rules`를 확인합니다.

```sql
SELECT card_name, regex_pattern, enabled, priority
FROM sms_parser_rules
ORDER BY priority DESC;
```

가맹점 자동 추천이 안 되면 `merchant_rules`를 확인합니다.

```sql
SELECT pattern, category_id, priority
FROM merchant_rules
ORDER BY priority DESC;
```

정규식은 Java named group을 사용해야 합니다.

```text
(?<amount>[\d,]+)
(?<date>\d{1,2}/\d{1,2})
(?<time>\d{2}:\d{2})
(?<store>.+)
```

## 11. 현재 검증 상태

### 프론트엔드

실행 명령:

```bash
cd ~/claude/budget_book_codex
source ./activate-env.sh
cd frontend
npm run build
```

결과:

```text
성공
vite build 완료
PWA service worker 생성 완료
```

확인된 경고:

| 경고 | 영향 | 대응 |
|---|---|---|
| chunk 500KB 초과 경고 | 기능 영향 없음 | 라우트 단위 lazy loading 검토 |

PWA 산출물 확인:

```text
dist/sw.js 생성 확인
dist/manifest.webmanifest 생성 확인
/api 런타임 캐시 없음
/api, /h2-console navigate fallback 제외 확인
```

### 백엔드

실행 명령:

```bash
cd ~/claude/budget_book_codex
source ./activate-env.sh
cd backend
GRADLE_USER_HOME=../.gradle-cache ./gradlew test
```

결과:

```text
성공
BUILD SUCCESSFUL
```

인증 스모크 테스트:

```text
미인증 GET /api/accounts → 401
POST /api/auth/register → 200 + token
GET /api/auth/me + Bearer token → 200
인증 GET /api/accounts → 200
POST /api/auth/login → 200 + token
```

사용자별 데이터 분리 스모크 테스트:

```text
사용자 A 기본 계좌/카테고리 생성 확인
사용자 B 기본 계좌/카테고리 생성 확인
사용자 A 거래 생성 → A 목록에서 조회
사용자 B 거래 목록 → A 거래 미노출
사용자 B가 A 거래 상세 조회 → 404
```

자산/이체/체크카드 스모크 테스트:

```text
예금 A → 예금 B 이체 생성 → A 잔액 차감, B 잔액 증가
예금 외 계좌를 포함한 이체 생성 → 400
체크카드에 예금 A 연결 후 지출 생성 → 거래 계좌는 체크카드, 실제 차감 계좌는 예금 A
거래 API로 TRANSFER 생성 시도 → 400
/api/accounts/{id}/activities → 이체 출금/입금, 체크카드 연결 예금 지출 모두 조회
체크카드/신용카드 생성 시 초기 잔액이 들어와도 0으로 보정
신용카드 지출 생성 → balanceAccount null, 카드 잔액 0 유지, 사용 내역은 자산별 내역에서 조회
```

## 12. 다음 개발 우선순위

| 우선순위 | 작업 | 이유 |
|---|---|---|
| 1 | 거래 분할/첨부 UI 구현 | Entity는 있으나 프론트 기능은 미구현 |
| 2 | 반복거래 자동 생성 스케줄러 | RecurringRule API만 있고 자동 생성 로직은 아직 없음 |
| 3 | 신용카드 결제일/마감일 UI | Account 필드는 있으나 카드 결제 예정 화면 없음 |
| 4 | 공통 Modal/Form 컴포넌트화 | Accounts/Categories/Budgets 중복 감소 |
| 5 | 운영 DB 전환 | H2 → MySQL/PostgreSQL |
| 6 | 라우트 lazy loading | 프론트 chunk 크기 경고 개선 |

## 13. 빠른 문제 해결 표

| 증상 | 확인할 곳 | 해결 |
|---|---|---|
| 프론트 화면은 뜨는데 API 403/404 | `vite.config.ts`, `WebConfig.java` | dev 서버 `15173` 사용, 백엔드 실행 확인 |
| `/transactions/:id` 라우트 오류 | `App.tsx` | 라우트 등록 확인 |
| `/accounts/:id` 라우트 오류 | `App.tsx` | `AccountDetailPage` 라우트 등록 확인 |
| CSS가 텍스트처럼만 보임 | `main.tsx`, `index.css` | `./index.css`에 Tailwind import 확인 |
| PWA 변경이 반영 안 됨 | 앱 배너, 브라우저 Application 탭 | "지금 적용" 배너 사용, 필요 시 SW unregister + Clear site data |
| API 요청이 Workbox 오류로 실패 | `vite.config.ts`, `dist/sw.js` | `/api` 런타임 캐시가 없는지 확인 |
| Swagger `/v3/api-docs` 500 | `build.gradle` springdoc 버전 | `2.8.0` 유지 |
| H2 DB locked | 실행 중인 backend/H2 console | 프로세스 종료 후 재시도 |
| SMS 파싱 실패 | `sms_parser_rules` | 카드사별 regex 보강 |
| 거래 수정 후 목록 조건 초기화 | `TransactionsPage`, `TxCard`, `TransactionDetailPage` | URL query와 `state.from` 보존 확인 |
| 체크카드 지출 저장 실패 | `AccountsPage`, `TransactionService` | 체크카드에 연결 예금이 지정되어 있는지 확인 |
| 이체 저장 실패 | `AddTransactionPage`, `TransferService` | 보내는/받는 계좌가 모두 예금이고 서로 다른지 확인 |

## 14. 핵심 파일 바로가기

| 목적 | 파일 |
|---|---|
| 전체 라우팅 | `frontend/src/App.tsx` |
| 레이아웃/메뉴 | `frontend/src/components/AppLayout.tsx` |
| 거래 목록 | `frontend/src/routes/TransactionsPage.tsx` |
| 거래 상세/수정 | `frontend/src/routes/TransactionDetailPage.tsx` |
| 거래 추가 | `frontend/src/routes/AddTransactionPage.tsx` |
| 자산 목록/연결 예금 | `frontend/src/routes/AccountsPage.tsx` |
| 자산별 내역 | `frontend/src/routes/AccountDetailPage.tsx` |
| SMS 입력 | `frontend/src/routes/SmsPage.tsx` |
| 통계 | `frontend/src/routes/StatsPage.tsx` |
| 예산 | `frontend/src/routes/BudgetsPage.tsx` |
| Axios 설정 | `frontend/src/api/client.ts` |
| 백엔드 거래 API | `backend/src/main/java/com/mybudget/backend/controller/TransactionController.java` |
| 거래 비즈니스 로직 | `backend/src/main/java/com/mybudget/backend/service/TransactionService.java` |
| 계좌/자산별 내역 로직 | `backend/src/main/java/com/mybudget/backend/service/AccountService.java` |
| 이체 비즈니스 로직 | `backend/src/main/java/com/mybudget/backend/service/TransferService.java` |
| SMS 파서 | `backend/src/main/java/com/mybudget/backend/service/SmsParserService.java` |
| DB/JPA 설정 | `backend/src/main/resources/application.yml` |
| 초기 데이터 | `backend/src/main/resources/data.sql` |
